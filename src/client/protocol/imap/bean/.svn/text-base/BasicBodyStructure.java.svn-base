package client.protocol.imap.bean;

import java.util.HashMap;
import java.util.HashSet;

//BodyTypeBasic = MediaBasic SP BodyFields
//MediaBasic    = ((DQUOTE ("APPLICATION" / "AUDIO" / "IMAGE" / "MESSAGE" / "VIDEO") DQUOTE) / ImapString) SP MediaSubtype;
public class BasicBodyStructure extends BodyStructure {
	public BasicBodyStructure(String mediaType, String mediaSubtype) {
		setMimeType(mediaType, mediaSubtype);
	}	
}
